{
  "name": "buy-sedifex-functions",
  "private": true,
  "main": "lib/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test test/**/*.test.cjs"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^5.1.1",
    "@sedifex/integration-types": "file:../packages/sedifex-integration-types"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
